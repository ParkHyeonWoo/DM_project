<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.board.mappers.board">
	<select id="list" resultType="com.board.domain.BoardDTO">
		select
		rune_id, rune_name, rune_name_kr, rune_desc
		from
		runes_info
	</select>

	<select id="champ" resultType="com.board.domain.MainDTO">
		select champ_id, champ_name, champ_name_kr
		from champ_basic_info
	</select>
</mapper>